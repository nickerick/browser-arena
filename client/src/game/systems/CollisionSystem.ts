/**
 * Client-side collision system placeholder.
 * Collision resolution is currently authoritative on the server (GameRoom.resolveCollisions).
 * This will handle local collision feedback (e.g. hit effects, camera shake) when needed.
 */
export class CollisionSystem {}
